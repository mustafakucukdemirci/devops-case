apiVersion: v1
kind: Secret
metadata:
  name: server-secrets
  namespace: mern-app
type: Opaque
stringData:
  # CI/CD replaces this value at deploy time via envsubst or Helm --set
  # Never commit a real connection string here
  ATLAS_URI: "mongodb://mongodb:27017/sample_training"
